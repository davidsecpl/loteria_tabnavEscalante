import React from "react";
import { View, Text } from "react-native";
import {styles} from './styles';

const PayTable = ()=>{
    return (
        <View style={styles.container}>
            <Text>Pantalla Tabla de Pagos</Text>
        </View>
    );
};

export default PayTable;